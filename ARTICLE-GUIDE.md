# 文章写作交接说明

本站是造价工程师 **ZhangWex** 的个人主页(<https://zhangwier.github.io/>),博客文章全部是 Markdown 文件。
你的任务:按下面的规范写一篇新文章,**直接输出完整的 `.md` 文件内容**。

不要输出开场白、不要解释你打算怎么写、不要用代码块把整篇文章包起来。第一行就是 `---`。

---

## 一、交付物

一个完整的 Markdown 文件,保存为 `content/posts/<slug>.md`。

`<slug>` 同时是文章的网址路径,用**小写英文 + 连字符**:`cost-splitting.md` → `/blog/cost-splitting/`。

## 二、frontmatter(必须有)

文件第一行开始,三个短横线包围:

```yaml
---
title: 文章标题
date: 2026-09-22
tags: [造价智能化, 工程量清单, 复核]
summary: 40 到 70 字的一句话摘要，会显示在文章列表页和首页。
---
```

- `title`:不超过 40 字。标题里有冒号时用中文全角「:」
- `date`:`YYYY-MM-DD`
- `tags`:2 到 4 个,中文优先
- `summary`:一句话讲清这篇解决了什么问题,不要写成目录

## 三、格式硬约束

1. **正文标题从 `##` 开始,绝不要用 `#`**。文章标题已由 frontmatter 的 `title` 渲染成页面标题,正文再用 `#` 会出现两个一模一样的标题。
2. 标题层级最多三层:`##` → `###`。
3. **表格最多 3 列**。正文阅读宽度约 768px,4 列以上会挤成一团。表格必须是标准 GFM 格式,第二行是 `|---|---|` 分隔行。
4. **不要写行内 HTML**。不支持,写了也不生效。
5. **不支持**脚注语法 `[^1]`、数学公式 `$...$`、MDX/JSX 组件。
6. **可以用**:标题、段落、**加粗**、*斜体*、有序/无序列表、引用 `>`、表格、行内代码、代码块、分割线 `---`、链接。
7. **图片**单独成行、前后各空一行,路径写成 `/images/文件名.svg`。
8. 段落之间空一行。中文正文用中文标点。术语里的英文缩写保留原样(RAG、UHPC、WBS)。

## 四、写作风格

**语气**:专业、克制、有判断力。像一个做了几年造价的人在写工作笔记——不是在写汇报,也不是在写公众号。

**要**:

- 开门见山说清要解决什么问题
- 用造价工作的真实场景举例:清单编制、定额套用、组价、变更签证、计量支付、结算审核、概算分解、审计
- 给出可操作的结论,而不是正确的废话
- 可以用第一人称("在实际工作中,我更关心的是……")
- 术语必须准确。工程量清单、定额套用、综合单价、组价、计量支付、变更签证、结算审核、概算分解、招标控制价——用不准不如不用

**不要**:

- AI 套话:"在当今快速发展的……"、"随着……的不断深入"、"综上所述"、"赋能"、"抓手"、"助力"、把"闭环"当口头禅
- 空泛排比、三段式对仗、每段都配小标题(标题是给结构用的,不是装饰)
- **编造数据**。任何数字都要有出处;确实是估计的,就明说"通常""一般"
- 煽情的个人感悟和鸡汤式结尾
- 过度的小标题化:一篇文章 3 到 5 个 `##` 就够

**引用外部资料**时:正文用 `[1]` `[2]` 标注,文末加一节 `## 参考来源`,用无序列表逐条列出。

## 五、内容红线

1. **所有真实项目必须匿名化**。写成"某高速公路项目""某跨海通道项目""某市政道路项目"。**不得出现**具体项目名、标段号(TJ2 标这类)、地名、业主或施工单位名称。
2. 不得出现未公开的造价数据、合同金额、清单单价、图纸内容、内部系统名称。
3. 不得出现同事姓名或任何个人信息。
4. 涉及单位内部做法时,用"某造价咨询单位""常见的做法是"这类表述。
5. 拿不准是否敏感的内容,一律不写。

## 六、篇幅与结构

- **1500 到 3000 字**为宜
- 推荐结构:引言(问题)→ 分点展开(每点一个 `##`)→ 结论(可落地的一句话)
- 观点密度比篇幅重要,不要为凑字数重复

## 七、提交前自查

- [ ] frontmatter 四个字段齐全,`date` 是 `YYYY-MM-DD`
- [ ] 正文里没有 `#` 一级标题
- [ ] 没有超过 3 列的表格
- [ ] 没有行内 HTML、脚注、数学公式
- [ ] 项目名全部匿名化,没有可识别的单位、地点、编号
- [ ] 没有编造的数字
- [ ] 引用了外部资料的话,文末有「参考来源」

## 附:已发布文章的格式样板

下面是本站现有文章的骨架,你的输出结构应当与此一致。

```markdown
---
title: 造价知识中枢（CostRAG）：RAG在工程造价领域的应用与落地方案
date: 2026-09-21
tags: [造价智能化, RAG, 知识库, 落地方案]
summary: 从 RAG 的基本原理讲到造价场景中的四类应用，再给出可落地的四层架构与部署方案。
---

## 引言

工程造价任务的核心特征是确定性检索与精确执行，而非开放性推理。通用大模型在这类任务中普遍存在三个问题：检索误差、计算失准、格式失控。本文的任务是：……

## 一、RAG的基本内涵与优势

### （一）RAG是什么

检索增强生成（Retrieval-Augmented Generation，RAG）是一种让大语言模型在生成回答之前，先检索外部知识库中最相关信息，再基于检索结果生成回答的技术架构。

| 对比维度 | 直接将文件夹交给AI | 使用RAG知识库 |
|---|---|---|
| 工作方式 | 全量输入，模型自行阅读 | 切片建索引，按需检索 |
| 查找精度 | 模型自行翻找，易漏、易混 | 关键词、语义与重排结合，精准召回 |

## 二、……

## 四、结论

……通过约束固化专业经验，使重复性造价工作具备可复现性与可靠性。

## 参考来源

- [1] 人工智能技术在工程造价领域的应用与实践，《工程造价管理》2026年第1期。
- [2] Intelligent Cost Analysis and Review System for Power Engineering，IEEE，2025。
```

完整例子见仓库里的 `content/posts/costrag.md`,那是本站的格式基准。
